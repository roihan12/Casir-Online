// hooks - Public API
// Re-export all global hooks

export { useAuth, default as useAuthHook } from './useAuth';

// Add more global hooks as needed
// These are hooks that are used across multiple features
